#Types
type User {
	id: String
	user_type: String
}

type AuthenticationType {
	token: String
	refreshToken: String
	user: User
}

#Queries
# type Query {}

#Mutations
type Mutation {
	login(	username: String!, password: String!): AuthenticationType
	refreshToken: AuthenticationType
}
