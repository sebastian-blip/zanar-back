
const erp = {
    USERNAME: process.env.ERP_USERNAME || 'demo',
    PASSWORD: process.env.ERP_PASSWORD || 'demo',
    HOST: process.env.ERP_HOST || 'localhost',
    TIMEOUT: 3000
}

export default erp;